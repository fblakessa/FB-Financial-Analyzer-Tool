# Local SQLite database file. The run/reset scripts export an absolute path
# automatically; this relative default works when you run Prisma or `next dev`
# from the repo root. No other environment values are needed — the template has
# no auth, no API keys, and no external services.
DATABASE_URL="file:./dev.db"
